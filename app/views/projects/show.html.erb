<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @project.name %>
</p>

<% if @project.default_language then %>
<p>
  <b>Default language:</b>
  <%= t('languages.' + @project.default_language) %>
</p>
<% end %>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
